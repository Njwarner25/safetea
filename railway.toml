[build]
builder = "nixpacks"

[deploy]
startCommand = "node server/db/migrate.js && node server/index.js"
healthcheckPath = "/api/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
