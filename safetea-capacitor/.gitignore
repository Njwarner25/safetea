node_modules/
# Native projects are generated by `cap add` — keep them out of git until first commit.
# After running `cap add ios && cap add android`, remove these two lines to commit the native projects.
ios/
android/

# Build artifacts
dist/
*.xcarchive
*.ipa
*.apk
*.aab

# Generated assets / staging files (re-derived from brand logo source)
.source-icon-1024.png
.source-brand-logo.png
.cup-rough.png
.staging-web-icons/
icons/
www/manifest.json

# Keystores — NEVER commit
*.keystore
*.jks
*.p12
*.mobileprovision
GoogleService-Info.plist
google-services.json

.DS_Store
